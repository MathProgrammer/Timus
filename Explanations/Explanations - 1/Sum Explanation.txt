
--------------------------------------------------

If n > 0, n*(n + 1)/2
else, 1 - sum(abs(n))

------------------------------

int main()
{
	int n;
	scanf("%d", &n);

	int sum = (n > 0 ? n*(n + 1)/2 : 1 - ( (-n)*(-n + 1)/2 ) );

	printf("%d\n", sum);
	return 0;
}